# name: test/sql/delta_export.test
# group: [sql]

# Require the extension to be loaded
require delta_export

# Verify the export_delta table function is registered
query I
SELECT count(*) FROM duckdb_functions() WHERE function_name = 'export_delta' AND function_type = 'table';
----
1
