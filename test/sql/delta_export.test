# name: test/sql/delta_export.test
# group: [sql]

# Require the extension to be loaded
require delta_export

# Test that the function exists (will error because no ducklake metadata tables exist)
statement error
CALL export_delta();
----
